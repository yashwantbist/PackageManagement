#include "typeTest.h"

// Check if contents is public
bool has_public_contents(const Package& pkg) {
    return false; 
}

// Check if length is public
bool has_public_length(const Package& pkg) {
    return false; 
}

// Check if width is public
bool has_public_width(const Package& pkg) {
    return false; 
}

// Check if height is public
bool has_public_height(const Package& pkg) {
    return false; 
}
