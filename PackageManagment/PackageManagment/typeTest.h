#ifndef TYPETEST_H
#define TYPETEST_H

#include "package.h"

bool has_public_contents(const Package& pkg);
bool has_public_length(const Package& pkg);
bool has_public_width(const Package& pkg);
bool has_public_height(const Package& pkg);

#endif 
